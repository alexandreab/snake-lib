{
  "name": "snake-lib",
  "version": "1.0.0",
  "description": "Library of methods for snake game simulation",
  "author": "Alexandre Barbosa",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.8.3"
  },
  "devDependencies": {
    "@types/jest": "^29.0.3",
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "jest": "^29.0.3",
    "ts-jest": "^29.0.2",
    "typescript": "~4.7.0"
  },
  "scripts": {
	"test": "npx jest",
	"coverage": "npx jest --coverage --coverageReporters=html --coverageReporters=text-summary",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "lint": "gts lint",
    "posttest": "npm run lint",
    "prepare": "npm run compile",
    "pretest": "npm run compile"
  }
}
